/**
 * Optimized Components
 * 
 * Performance-optimized versions of common CRM components with
 * memoization, virtual scrolling, and intelligent re-rendering.
 */

// Optimized data table
export {
  OptimizedDataTable,
  useOptimizedColumns,
  useChunkedData,
  useSmartTableUpdates,
  generateTestData,
  useTableBenchmark,
} from './OptimizedDataTable'