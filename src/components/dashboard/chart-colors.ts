// Chart color configuration for harmonized theming
export const chartColors = {
  primary: 'hsl(var(--chart-primary))', // MFB Green
  success: 'hsl(var(--chart-success))', // Success Green
  warning: 'hsl(var(--chart-warning))', // Warning Amber
  info: 'hsl(var(--chart-info))', // Info Blue
  accent: 'hsl(var(--chart-accent))', // Purple accent
  muted: 'hsl(var(--muted))', // Light gray

  // Series colors for multi-dataset charts
  series: [
    'hsl(var(--chart-primary))', // MFB Green
    'hsl(var(--chart-success))', // Success Green
    'hsl(var(--chart-info))', // Info Blue
    'hsl(var(--chart-accent))', // Purple accent
    'hsl(var(--chart-warning))', // Warning Amber
  ],
}
